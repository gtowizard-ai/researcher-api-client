[project]
name = "researcher-api"
version = "0.1.0"
description = "GTO Wizard AI researcher API Client"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fire>=0.7.1",
    "httpx>=0.28.1",
    "pre-commit>=4.5.1",
    "structlog>=25.5.0",
    "tqdm>=4.67.1",
]
